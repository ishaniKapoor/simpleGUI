User opens GUI
↓
Selects conversion: miles → km, lbs → kg, fahrenheit → celsius, etc.
↓
GUI sends request to backend server
↓
Server calculates result
↓
GUI displays result

Server
- do the api calls to calculate result
- send result back to client

Client
- allow user to make selections
- send request with values to server